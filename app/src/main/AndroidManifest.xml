<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.caus_abdellah.whatfordinner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Copyright_Activity"
            android:label="@string/title_activity_copyright_"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewDish_activity" />
        <activity android:name=".Grocery_activity" />
        <activity android:name=".Meals_activity" />
        <activity android:name=".Recipe_activity" />
        <!--<activity android:name="nonscrollListVew.NonScrollListViewActivity"></activity>-->
    </application>

</manifest>